{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "installCommand": "if [ -d \"frontend\" ]; then cd frontend && npm install; else npm install; fi",
  "buildCommand": "if [ -d \"frontend\" ]; then npm run build; else npm run build; fi",
  "framework": "nextjs"
}
